# Мини-оболочка на Python

## Общий алгоритм работы программы

Программа представляет собой интерактивную командную оболочку, имитирующую работу стандартного терминала. Основной цикл работы:

1. **Инициализация** - настройка логирования и подготовка окружения
2. **Основной цикл**:
   - Отображение текущей директории в приглашении ввода
   - Чтение команды от пользователя
   - Разбивка ввода на команду и аргументы
   - Поиск и выполнение соответствующей команды
   - Логирование операции и результата
3. **Завершение** - корректный выход по команде `exit` или Ctrl+C

Архитектура разделена на три основных слоя:
- **file_commands** - низкоуровневые операции с файловой системой
- **auxiliary_functions** - парсинг аргументов и вызов соответствующих команд  
- **additional_functions** - вспомогательные утилиты и проверки

## Нюансы реализации команд

### `ls` - просмотр содержимого
- Поддерживает два режима: простой (только имена) и подробный (`-l` с размером, временем и правами)
- Может работать как с директориями, так и с отдельными файлами
- Автоматически определяет текущую директорию если путь не указан
- Для Windows эмулирует права доступа в Unix-стиле(но делает это с переменным успехом :3)

### `cd` - смена директории
- Специальные пути: `~` (домашняя директория), `..` (родительская), `~path` (относительно домашней)
- Проверяет что целевой путь существует и является директорией(если 2 аргумента и оба файлы, то записывает 1й файл во 2й)

### `cat` - просмотр файлов
- Поддерживает просмотр нескольких файлов за один вызов
- Проверяет что путь ведет к файлу, а не директории

### `cp` - копирование
- Рекурсивное копирование директорий с опцией `-r`
- Поддерживает копирование нескольких источников в одну целевую директорию
- Защита от копирования в самого себя
- Сохраняет метаданные файлов при копировании

### `mv` - перемещение/переименование
- Единая логика для перемещения и переименования
- Проверяет совместимость типов (нельзя переместить директорию в файл)
- Обрабатывает перемещение нескольких файлов в директорию

### `rm` - удаление
- Рекурсивное удаление директорий с опцией `-r`
- Подтверждение для удаления директорий(y/n)
- Защита системных путей от случайного удаления

## Запуск проекта

### Требования:
- Python, какой не знаю, пусть будет 3.10, как у меня, хотя даже 3.6 должен подходить
- Доступ к файловой системе с правами на чтение/запись

### Запуск:

```bash

python -m src.main
```

### Структура проекта после запуска:
```
project/
├── src/
│   ├── additional_functions/ # дополнительные функции
│   │   ├── __init__.py
│   │   ├── find_function.py
│   │   ├── functions_for_is.py
│   │   └── is_directory_protected.py
│   ├── auxiliary_functions/ # функции обработки сообщения пользователя
│   │   ├── cat.py
│   │   ├── cd.py
│   │   ├── cp.py
│   │   ├── ls.py
│   │   ├── mv.py
│   │   └── rm.py
│   ├── file_commands/ # функции выыполнения команды
│   │   ├── cat_command.py
│   │   ├── cd_command.py
│   │   ├── cp_command.py
│   │   ├── ls_command.py
│   │   ├── mv_command.py
│   │   ├── rm_command.py
│   │   └── __init__.py
│   ├── __init__.py
│   ├── loggins.py
│   ├── main.py
│   └── shell.log
├── tests/ # тесты
├── .gitignore
├── .pre-commit-config.yaml
├── Dockerfile
├── pyproject.toml
├── pytest.ini
├── README.md
└── uv.lock
```

## Запуск тестов

### Установка зависимостей для тестирования:
```bash
pip install pytest pytest-mock pyfakefs
```

### Запуск тестов:

```bash
# Все тесты
pytest

# С подробным выводом
pytest -v

# Конкретная группа тестов
pytest tests/test_file_commands/ -v
pytest tests/test_auxiliary_functions/ -v

# Тесты с покрытием кода
pytest --cov=src

# Запуск определенного теста
pytest tests/test_file_commands/test_ls_command.py::TestCommandLsImplementation::test_ls_current_directory -v
```

### Структура тестов:
```
tests/
|-- test_additional_functions/ # тесты дополнительных функций
|   |-- test_functions_for_is.py
|   `-- test_is_directory_protected.py
|-- test_auxiliary_functions/ # тесты функций обработки сообщения пользователя
|   |-- test_cat.py
|   |-- test_cd.py
|   |-- test_cp.py
|   |-- test_is.py
|   |-- test_mv.py
|   `-- test_rm.py
|-- test_file_commands/ # тесты функций выыполнения команды
|   |-- test_cat_command.py
|   |-- test_cd_command.py
|   |-- test_cp_command.py
|   |-- test_is_command.py
|   |-- test_mv_command.py
|   `-- test_rm_command.py
|-- __init__.py
`-- conftest.py Штука, которая создаёт окружение для тестов
```

### Особенности тестирования:
- Используется `pyfakefs` для изолированного тестирования файловых операций
- Мокируются системные вызовы для предсказуемости тестов
- Каждая команда тестируется на успешные и ошибочные сценарии

Тесты покрывают все основные сценарии использования.𓅬𓅬𓅬

### Допущения:
- Информация о разрешениях на windows скорее всего будет ложная, потому что это винда со своим униКАЛьным форматом разрешений (◕‿◕)
- За правильность работы Docker файла не отвечаю, но у меня в начале работы над проектом работал (ᵔ⩊ᵔ)
- Я надеюсь, что лягушёнок снизу не растянулся

⣿⣿⣿⣿⣿⣿⣿⠛⢩⣴⣶⣶⣶⣌⠙⠫⠛⢋⣭⣤⣤⣤
⣿⣿⣿⣿⣿⡟⢡⣾⣿⠿⣛⣛⣛⣛⣛⡳⠆⢻⣿⣿⣿⠿⠿⠷⡌
⣿⣿⣿⣿⠏⣰⣿⣿⣴⣿⣿⣿⡿⠟⠛⠛⠒⠄⢶⣶⣶⣾⡿⠶⠒⠲⠌
⣿⣿⠏⣡⢨⣝⡻⠿⣿⢛⣩⡵⠞⡫⠭⠭⣭⠭⠤⠈⠭⠒⣒⠩⠭⠭⣍⠒⠈
⡿⢁⣾⣿⣸⣿⣿⣷⣬⡉⠁⠄⠁⠄⠄⠄⠄⠄⠄⠄⣶⠄⠄⠄⠄⠄⠄⠄⠄⢀
⢡⣾⣿⣿⣿⣿⣿⣿⣿⣧⡀⠄⠄⠄⠄⠄⠄⠄⢀⣠⣿⣦⣤⣀⣀⣀⣀⠄
⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣷⣶⣶⡶⢇⣰⣿⣿⣟⠿⠿⠿⠿⠟
⣿⣿⣿⣿⣿⣿⣿⡟⢛⡛⠿⠿⣿⣧⣶⣶⣿⣿⣿⣿⣿⣷⣼⣿⣿⣿⣧
⠘⢿⣿⣿⣿⣿⣿⡇⢿⡿⠿⠦⣤⣈⣙⡛⠿⠿⠿⣿⣿⣿⣿⠿⠿⠟⠛⡀
⠄⠄⠉⠻⢿⣿⣿⣷⣬⣙⠳⠶⢶⣤⣍⣙⡛⠓⠒⠶⠶⠶⠶⠖⢒⣛⣛⠁
⠄⠄⠄⠄⠄⠈⠛⠛⠿⠿⣿⣷⣤⣤⣈⣉⣛⣛⣛⡛⠛⠛⠿⠿⠿⠟⢋
⠄⠄⠄⠄⠄⠄⠄⠄⠄⠄⠄⠄⠈⠉⠉⣻⣿⣿⣿⣿⡿⠿⠛⠃⠄⠙

